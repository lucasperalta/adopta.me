<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <!-- "Mark Favorite", should appear as action button if possible -->

    <item
        android:id="@+id/actionAgregarMascotas"
        android:icon="@drawable/ic_cloud_upload_2x"
        android:title="Agregar Mascota"
        app:showAsAction="always"/>

    <item
        android:id="@+id/actionMisMascotas"
        android:icon="@drawable/ic_pets_black_24dp"
        android:title="mis mascotas"
        app:showAsAction="always"/>

    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/actionFiltros"
        android:icon="@drawable/ic_settings_black_24dp"
        android:title="filtros"
        app:showAsAction="always"/>
    <item
        android:id="@+id/actionLogout"
        android:icon="@drawable/power"
        android:title="logout"
        app:showAsAction="always"/>
</menu>